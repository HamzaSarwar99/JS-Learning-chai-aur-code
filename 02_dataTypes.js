"use strict";// java script treat all our code as the newer version of js

//Primitive dataTypes

let name = "hamza" // string datatype

let age = 20 // number => range =>2 to the power 53 round about

// if a number exceed this than we will use bigint

let isLoggedIn = false //boolean => true/false

let temperature = null

// null this is a standalone value

let state;// undefined 

symbol //used for uniqueness


//nonPrimitive dataTypes

//object

console.log(typeof null)//this will give object which is defeciency in js

console.log(typeof undefined)//this will give undefined as we don't know
//it's value

