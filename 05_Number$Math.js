const score = 100;
//console.log(score.toFixed(2))// 100.00 used for precision
// toprecision() is used for rounding off

const number = 106

// console.log(number.toString())
// console.log(typeof number);

const hundereds = 100000;
//console.log(hundereds.toLocaleString('en-Pk'));


// ++++++++++++++++++++++++///////////++++++++++++++++++++++++++++++++++++++

//Math inn java script

// console.log(Math); //this is an object itself

// console.log(Math.PI);//return the value of pi

const x = 100.09

// console.log(Math.trunc(x));// return the only integral part of the x 
// it will remove any decimal value to integral value


const y = 45.85
// console.log(Math.ceil(y));//46 it return the greater value doesn't matter whats
// the next value after decimal,


const z = 45.85

// console.log(Math.floor(y));//45 it is the opposite of the ceil

const absoluteVal = -7

// console.log(Math.abs(absoluteVal)); // it convert the minus into plus but
// will not convert plus into minus 

// console.log(Math.round(8.7)); //9 

// it obeys the rounding off same as in math

// console.log(Math.min(5, 3, 8, 1, 6)); // 1

// it gives the lowest value

// console.log(Math.max(4, 7, 8, 9, 3)); // 9

// it gives the highest value
 
// console.log(Math.random()); it gives a random value between 0 (inclusive) 

// and 1(exclusive)


const maxValue = 40

const minValue = 20

// console.log(Math.floor(Math.random() * (maxValue - minValue + 1)) + minValue)
// this formula will generate a value between 20 and 40 

const min = 90

const max = 100

// console.log(Math.floor(Math.random()*(max-min + 1))+min)

